# Place a macOS SDK tarball here, e.g. MacOSX11.3.sdk.tar.xz
# (extracted from Xcode via osxcross's tools/gen_sdk_package*.sh — see ../../README.md).
# The tarball is intentionally NOT committed: Apple licenses the SDK for Apple hardware only.
